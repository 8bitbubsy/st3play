#pragma once

#define SINC_TAPS 16
#define SINC_TAPS_BITS 4 /* log2(SINC_TAPS) */
#define SINC_OVERSAMPLING 256
#define SINC_OVERSAMPLING_BITS 8 /* log2(SINC_OVERSAMPLING) */

#define RESAMPLING_FRAC_BITS 32
#define RESAMPLING_FRAC_SCALE (1ULL << RESAMPLING_FRAC_BITS)
#define RESAMPLING_FRAC_MASK (RESAMPLING_FRAC_SCALE-1)

#define INTRP_PHASE_SHIFT (RESAMPLING_FRAC_BITS-SINC_OVERSAMPLING_BITS)
#define INTRP_PHASE_SCALE (1L << INTRP_PHASE_SHIFT)
#define INTRP_PHASE_MASK (INTRP_PHASE_SCALE-1)

extern const float fSincLUT[(SINC_OVERSAMPLING+1) * SINC_TAPS];
